# lumenpage-extension-collaboration

> 目录：`packages/extensions/extension-collaboration`

Yjs 协作文档同步扩展。

## 当前职责

- 把 `Y.Doc / Y.XmlFragment` 绑定到 `lumenpage-state` 文档。
- 提供协作态下的 `undo / redo` 命令入口。
- 暴露相对位置与文档互转工具，供协作光标扩展复用。
