// 统一组装 _internals，避免构造函数末尾对象字面量过长且难维护。
export const createEditorInternals = ({
  dom,
  settings,
  renderer,
  onChange,
  layoutPipeline,
  renderSync,
  getText,
  getLayout,
  getLayoutIndex,
  getRafId,
  getEditorProps,
  setEditorProps,
  setPendingChangeSummary,
  dispatchTransaction,
  updateLayout,
  updatePluginViews,
  syncNodeViews,
  destroyNodeViews,
  destroyPluginViews,
  clearDomEventHandlers,
  updateA11yStatus,
  scheduleRender,
  updateCaret,
  setInputPosition,
  syncNodeViewOverlays,
  syncAfterStateChange,
  detachInputBridge,
  onScroll,
  onResize,
  onClickFocus,
  onDoubleClick,
  onRootFocus,
  onDocumentSelectionChange,
  resetComposing,
  handlePointerDown,
  handlePointerMove,
  handlePointerUp,
  handleTouchStart,
  handleTouchMove,
  handleTouchEnd,
  handleTouchCancel,
  handleDragStart,
  handleDragOver,
  handleDragLeave,
  handleDrop,
  handleDragEnd,
  dragHandlers,
  refreshDomEventHandlers,
  getEditorPropsList,
  queryEditorProp,
  applyViewAttributes,
  inputDebugHandlers,
  setNodeSelectionAtPos,
  resolveGapSelectionAtPos,
  setGapCursorAtCoords,
}) => {
  return {
    dom,
    settings,
    renderer,
    onChange,
    layoutPipeline,
    renderSync,
    getText,
    getLayout,
    getLayoutIndex,
    getRafId,
    getEditorProps,
    setEditorProps,
    setPendingChangeSummary,
    dispatchTransaction,
    updateLayout,
    updatePluginViews,
    syncNodeViews,
    destroyNodeViews,
    destroyPluginViews,
    clearDomEventHandlers,
    updateA11yStatus,
    scheduleRender,
    updateCaret,
    setInputPosition,
    syncNodeViewOverlays,
    syncAfterStateChange,
    detachInputBridge,
    onScroll,
    onResize,
    onClickFocus,
    onDoubleClick,
    onRootFocus,
    onDocumentSelectionChange,
    resetComposing,
    handlePointerDown,
    handlePointerMove,
    handlePointerUp,
    handleTouchStart,
    handleTouchMove,
    handleTouchEnd,
    handleTouchCancel,
    handleDragStart,
    handleDragOver,
    handleDragLeave,
    handleDrop,
    handleDragEnd,
    dragHandlers,
    refreshDomEventHandlers,
    getEditorPropsList,
    queryEditorProp,
    applyViewAttributes,
    inputDebugHandlers,
    setNodeSelectionAtPos,
    resolveGapSelectionAtPos,
    setGapCursorAtCoords,
  };
};
